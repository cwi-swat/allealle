// N == 10

Queen (1 :: x:int, y:int) = {<q1,?,?>..<q10,?,?>}

Queen::x >= 1 && Queen::x <=10
Queen::y >= 1 && Queen::y <=10

// Queens cannot be on the same row, column or diagonal
forall q:Queen, q':Queen\q | q::x != q'::x && q::y != q'::y && |q::x - q'::x| != |q::y - q'::y|
