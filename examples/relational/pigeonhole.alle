Pigeon (pId:id)            = {<p1>..<p2>}
Hole   (hId:id)            = {<h1>..<h1>}
nest   (pId:id, hId:id)   <= {<p1,h1>..<p2,h1>}

//nest ⊆ Pigeon ⨯ Hole

//∀ p ∈ Pigeon | one   p ⨝ nest
∀ h ∈ Hole | lone  h ⨝ nest
