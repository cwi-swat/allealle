Pigeon (pId:id)            = {<p1>..<p2>}
Hole   (hId:id)           <= {<h1>..<h2>}
nest   (pId:id, hId:id)   >= {<p1,h1>} <= {<p1,h1>..<p2,h2>}

∀ p ∈ Pigeon | one   p ⨝ nest
∀ h ∈ Hole | lone  h ⨝ nest

nest ⊆ Hole ⨯ Pigeon