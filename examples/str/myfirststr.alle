Person (pId:id, name:str, age:int) <= {<p1,?,37>, <p2,?,?>}

∃ p ∈ Person | some p where (name = toStr(34) && age = length("aaaaaaaaaaaa"))
∃ p ∈ Person | some p where (name = "Bob" ++ "by" && length(name) = toInt("5"))
∃ p ∈ Person | some p where (substr(name, 0,2) = "Bo")
 
objectives: minimize Person[count()]

